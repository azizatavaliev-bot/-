require('./lib/env');

// Настройки лендинга с таймером на 24 часа.
// Всё, что нужно менять под свой оффер, находится в этом файле.
module.exports = {
  // Сколько времени даётся человеку с момента первого захода на сайт.
  // Отсчёт идёт на сервере и продолжается, даже когда вкладка закрыта.
  windowHours: 24,

  // Порт HTTP-сервера
  port: Number(process.env.PORT) || 3000,

  // Ключ для входа в /admin (обязательно смените в .env)
  adminKey: process.env.ADMIN_KEY || 'change-me',

  // Как долго браузер помнит выданный ему идентификатор.
  // Должно быть заметно больше windowHours, иначе после истечения cookie
  // человек получит новые 24 часа.
  cookieDays: 90,

  brand: {
    name: 'Название проекта',
    title: 'Заявка открыта ровно 24 часа',
    subtitle:
      'С момента, как вы открыли эту страницу, у вас есть 24 часа, чтобы заполнить анкету. Таймер идёт непрерывно — даже если закрыть сайт.',
    // Блоки «что вы получите» — можно менять, добавлять, удалять
    bullets: [
      'Разбор вашей ситуации и точек роста',
      'Пошаговый план на ближайший месяц',
      'Ответ в течение рабочего дня после анкеты',
    ],
    submitLabel: 'Отправить анкету',
    successTitle: 'Анкета принята',
    successText: 'Мы получили ваши ответы и свяжемся с вами по указанным контактам.',
    expiredTitle: 'Время вышло',
    expiredText:
      'Отведённые 24 часа закончились, приём анкеты по этой ссылке закрыт. Если вам всё ещё актуально — напишите нам напрямую.',
    footer: '© ' + new Date().getFullYear() + ' Название проекта',
  },

  // Поля анкеты. type: text | tel | email | textarea | select | number
  // required: true — поле проверяется и на клиенте, и на сервере.
  fields: [
    { name: 'name', label: 'Как вас зовут', type: 'text', required: true, maxLength: 80 },
    { name: 'phone', label: 'Телефон (WhatsApp)', type: 'tel', required: true, maxLength: 30, placeholder: '+996 700 000 000' },
    { name: 'city', label: 'Город', type: 'text', required: false, maxLength: 80 },
    {
      name: 'niche',
      label: 'Чем вы занимаетесь',
      type: 'select',
      required: true,
      options: ['Услуги', 'Товарный бизнес', 'Онлайн-школа / эксперт', 'Пока только идея', 'Другое'],
    },
    {
      name: 'revenue',
      label: 'Текущий оборот в месяц',
      type: 'select',
      required: true,
      options: ['Нет оборота', 'До 100 000', '100 000 – 500 000', '500 000 – 2 000 000', 'Больше 2 000 000'],
    },
    {
      name: 'goal',
      label: 'Какой результат хотите получить',
      type: 'textarea',
      required: true,
      maxLength: 1500,
      placeholder: 'Опишите задачу своими словами',
    },
  ],
};
